# המשרד שלי - הוראות לקלוד

התיקייה הזו היא **"המשרד שלי"**: דשבורד-על שמגלה ומציג את כל הסקילים, הסוכנים, החיבורים
והמשימות המתוזמנות של המשתמש, ומאפשר להפעיל סקילים ולקבל דיווחים מאוטומציות.

## אם המשתמש רק התקין את זה

עזור לו להתחיל, בסדר הזה:

1. הרץ `./setup.sh` (ווינדוס: `setup.bat`) והצג לו את המונים שהגילוי החזיר.
2. הפעל את השרת ברקע ופתח לו את הדפדפן על `http://127.0.0.1:17090`.
3. **ראיין אותו על המערכות שלו** וכתוב `systems.json`: איפה הלקוחות רשומים, איפה הכסף מנוהל,
   איפה השיווק, איפה המסמכים, אילו דשבורדים מקומיים יש לו, ואילו אוטומציות רצות במקום אחר.
   שאלה אחת בכל פעם. הצג טבלה לאישור לפני שאתה כותב. **לעולם לא סיסמאות או מפתחות בקובץ הזה.**
4. הצע לו את ה-hook לרישום אוטומטי (README, "רישום אוטומטי") והוסף אותו ל-`~/.claude/settings.json`
   במיזוג - בלי למחוק hooks קיימים, עם נתיב מלא, ואמת שה-JSON תקין.

## כללי ברזל בפרויקט הזה

1. **אף מפתח, טוקן, סיסמה או כתובת מייל לא נכנסים ל-`registry.db` ולא ל-`systems.json`.**
   חיבורי MCP נרשמים בשם ובסוג בלבד; ממשתני סביבה שומרים רק את שמות המפתחות.
2. **השרת מאזין רק ל-`127.0.0.1`.** אם המשתמש מבקש לפתוח אותו לרשת - הסבר שהדרך הנכונה היא
   מנהרת SSH (`ssh -L`) או רשת פרטית, ולא פתיחת פורט לאינטרנט.
3. **הרצת סקיל מהדשבורד היא `--permission-mode acceptEdits`** ולא bypass. סקיל שצריך יותר -
   מריצים בשיחה, בעיניים.
4. **stdlib בלבד.** אין `pip install`, אין ספריות חיצוניות, אין קריאות יוצאות מהשרת.
5. **הגילוי אידמפוטנטי.** פריט שנעלם מסומן `active=0` ולא נמחק; רישום ידני (`source='register'`)
   לא מסומן ולא נמחק לעולם.
6. שינוי במבנה הנתונים - גם ב-`discover.py` (הכתיבה) וגם ב-`server.py` (הקריאה) וגם ב-`index.html`.

## קבצים

| קובץ | תפקיד |
|---|---|
| `discover.py` | הגילוי - סורק סקילים, סוכנים, MCP, משימות מתוזמנות, hooks, systems.json |
| `server.py` | שרת + API (`/api/state`, `/api/run`, `/api/observe`, `/api/register`, ...) |
| `index.html` | הדשבורד - קובץ אחד, RTL, תפריט צד |
| `register.py` | רישום ידני של מה שאין לו גילוי |
| `office_hook.sh` | hook ל-PostToolUse שמריץ גילוי כשנכתב סקיל/סוכן/משימה |

## הרחבות נפוצות שהמשתמש יבקש

- **"תוסיף לשונית"** - נתונים ב-`discover.py`, הגשה ב-`server.py`, תצוגה ב-`index.html` (רשימת
  `KIND` ותפריט הצד).
- **"שיעלה לבד"** - LaunchAgent (מק), Task Scheduler (ווינדוס), systemd user service (לינוקס).
  תמיד להוכיח שזה רץ: הרשימה של המערכת + `curl /api/health` אחרי סגירת הטרמינל.
- **"שירוץ בשרת"** - `loginctl enable-linger`, והצפייה דרך `ssh -L` או לשונית PORTS ב-VS Code.
- **"שהסוכן ידווח"** - השורה של `POST /api/observe` בסוף הסקריפט של הסוכן.
